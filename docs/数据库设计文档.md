# 数据库设计文档

创建时间: 2026-03-12
创建者: TraeAI
任务: 生成数据库文档

修改时间: 2026-03-15
修改者: TraeAI
任务: postgresql-migration
修改内容: 迁移至 PostgreSQL，更新数据库连接配置和架构说明，添加 pgvector 支持

修改时间: 2026-03-16
修改者: TraeAI
任务: fix-disambiguation-three-phase
修改内容: 新增 disambig_checkpoint 表，用于人名消歧断点恢复

修改时间: 2026-03-29
修改者: TraeAI
任务: knowledge-graph-integration
修改内容: 新增图谱权威表（graph_entities、graph_entity_aliases、graph_relation_events、graph_relations_current、graph_checkpoints），更新 chunk_relations 字段

---

## 一、数据库概述

### 1.1 数据库类型

项目使用 **PostgreSQL** 作为数据存储引擎，具有以下特点：
- 生产级关系型数据库，支持高并发
- 支持复杂查询和事务处理
- 启用外键约束确保数据完整性
- 通过 `run_id` 实现多任务数据隔离
- **pgvector 扩展**：支持向量相似度搜索

### 1.2 数据库连接配置

数据库连接通过 `DATABASE_URL` 环境变量配置：

```bash
# 使用 psycopg (v3) 驱动
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/novel_analysis
```

### 1.3 pgvector 扩展

项目使用 **pgvector** 扩展进行向量相似度搜索：

```sql
-- 启用扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 向量列定义（1536维，适用于 OpenAI text-embedding-3-small）
embedding_vector vector(1536)

-- 创建 HNSW 索引（高效的近似最近邻搜索）
CREATE INDEX idx_chunk_embeddings_vector 
ON chunk_embeddings 
USING hnsw (embedding_vector vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

### 1.4 ID 体系

| ID 类型 | 格式 | 示例 | 用途 | 使用场景 |
|---------|------|------|------|----------|
| run_id | 完整UUID(36位) | `10960c77-1a72-4444-a772-2ddc64334cd2` | 分析运行标识（核心隔离键） | 仅内部实现使用 |
| novel_id | UUID前8位 | `10960c77` | 小说唯一标识 | API内外通用 |
| task_id | UUID前8位 | `a1b2c3d4` | 分析任务标识（外部使用） | 仅API层使用 |
| chunk_id | 自增整数 | `1, 2, 3...` | 文本块标识 | 内部使用 |
| entity_id | 自增整数 | `1, 2, 3...` | 实体标识 | 内部使用 |

**ID映射关系：**
- `task_id` 是 `run_id` 的前8位字符
- 外部API只暴露 `task_id`，完全隐藏 `run_id`
- 内部实现（Repository、Workflow）只使用 `run_id`
- ID转换由API层和Service层负责

### 1.5 数据隔离策略

所有业务表通过 `run_id` 外键关联 `analysis_runs` 表实现数据隔离：

```sql
-- analysis_runs 表结构
CREATE TABLE analysis_runs (
    run_id VARCHAR(36) PRIMARY KEY,
    novel_id VARCHAR(36) NOT NULL,
    status VARCHAR(20) DEFAULT 'running',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    source_path TEXT,
    novel_title TEXT
);

-- 所有业务表添加 run_id 外键
ALTER TABLE chunks ADD COLUMN run_id VARCHAR(36) REFERENCES analysis_runs(run_id) ON DELETE CASCADE;
```

---

## 二、数据库连接配置

```python
# 创建时间: 2026-03-15
# 创建者: TraeAI
# 任务: postgresql-migration
# 说明: SQLAlchemy 数据库连接配置

from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
import os

DATABASE_URL = os.getenv("DATABASE_URL", "postgresql+psycopg://postgres:postgres@localhost:5432/novel_analysis")

engine = create_engine(DATABASE_URL, pool_size=5, max_overflow=10)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

def get_session():
    """获取数据库会话"""
    session = SessionLocal()
    try:
        yield session
    finally:
        session.close()
```

---

## 三、表结构详解

### 3.1 核心数据表

#### 3.1.1 chunks - 文本块表

存储分块后的文本数据。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| chunk_id | INTEGER | PRIMARY KEY | 块ID，自增主键 |
| chapter_id | INTEGER | - | 章节ID |
| char_offset | INTEGER | - | 块文本在全文中的起始字符偏移 |
| char_end_offset | INTEGER | - | 块文本在全文中的结束字符偏移 |
| text | TEXT | NOT NULL | 块文本内容 |
| run_id | VARCHAR(36) | FK | 运行ID（外键关联 analysis_runs） |

**索引**: idx_chunks_run_id (run_id)

---

#### 3.1.2 chunk_style - 语言风格指标表

存储每个文本块的语言风格指标。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| mtld | REAL | 词汇多样性指标（Measure of Textual Lexical Diversity） |
| ttr | REAL | 类符/形符比（Type-Token Ratio） |
| avg_sent_len | REAL | 平均句长 |
| sent_len_std | REAL | 句长标准差 |
| d_value | REAL | 词汇密度D值 |
| pause_density | REAL | 停顿密度 |
| fight_density | REAL | 战斗词密度 |
| exclaim_density | REAL | 感叹词密度 |
| dialogue_ratio | REAL | 对话占比 |
| question_density | REAL | 疑问词密度 |
| sensory_density | REAL | 五感词密度 |
| metaphor_density | REAL | 比喻密度 |
| cultural_density | REAL | 传统文化密度 |
| function_word_vector | TEXT | 虚词向量（JSON格式） |
| category_density_combat | REAL | 战斗类别密度 |
| category_density_body | REAL | 身体类别密度 |
| category_density_relation | REAL | 关系类别密度 |
| category_density_faction | REAL | 势力类别密度 |
| category_density_command | REAL | 命令类别密度 |
| category_density_action | REAL | 动作类别密度 |
| category_density_psychology | REAL | 心理类别密度 |
| category_density_measure | REAL | 度量类别密度 |
| category_density_emotion | REAL | 情感类别密度 |
| category_density_color | REAL | 颜色类别密度 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.3 chunk_annotation - 语义标注表

存储本地模型对每个文本块的语义标注结果。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| emotional_valence | TEXT | 情感极性（五档枚举：strong_positive/mild_positive/neutral/mild_negative/strong_negative) |
| pivot_moment | INTEGER | 是否为转折点（0/1） |
| event_type | TEXT | 事件类型（三分类：冲突/铺垫/转折） |
| cliffhanger | INTEGER | 是否为悬念（0/1） |
| has_foreshadowing | INTEGER | 是否有伏笔（0/1） |
| foreshadowing_type | TEXT | 伏笔类型 |
| foreshadowing_desc | TEXT | 伏笔描述 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.4 chunk_characters - 人物信息表

存储每个文本块中出现的人物信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| name | TEXT | 人物名称 |
| role_function | TEXT | 角色功能（Greimas六元素：主体/客体/发送者/接收者/帮助者/反对者） |
| action | TEXT | 行为描述 |
| action_type | TEXT | 行为类型（七分类：战斗/逃跑/对话/决策/移动/情感/其他） |
| emotion_score | TEXT | 情绪强度（五档枚举：strong_positive/mild_positive/neutral/mild_negative/strong_negative） |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.5 chunk_relations - 人物关系表

存储每个文本块中的人物关系变化（Phase4 输出）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| from_char | TEXT | 关系发起方（原始称呼） |
| to_char | TEXT | 关系接收方（原始称呼） |
| type | TEXT | 关系类型：师徒/敌对/盟友/爱慕/家族/利益/主从 |
| change | TEXT | 关系变化：强化/弱化/新建/断裂/无变化 |
| evidence | TEXT | 原文证据片段（v10.6 新增） |
| confidence | REAL | Phase4 抽取置信度（v10.6 新增） |
| source_model | VARCHAR(100) | 模型来源（v10.6 新增） |
| normalized | INTEGER | 是否已投影入图谱表（0/1，v10.6 新增） |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)
**索引**: idx_chunk_relations_chunk (chunk_id), idx_chunk_relations_normalized (normalized)

**设计说明（v10.6）**：
- `from_char` / `to_char` 保存原始称呼，Phase4 面向原文，是证据提取阶段
- 正式名归一化由图谱投影阶段处理，结果写入 graph_relation_events
- `evidence` 字段支持证据追溯，每条关系都能追溯到原文
- `normalized` 标记是否已投影，避免重复处理

---

#### 3.1.6 chunk_dialogues - 对话信息表

存储每个文本块中的对话信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| speaker | TEXT | 说话者 |
| length | INTEGER | 对话长度 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.7 chunk_topics - 主题分布表

存储每个文本块的主题分布。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| topic_id | INTEGER | 主题ID |
| topic_weight | REAL | 主题权重 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.8 chunk_foreshadowing - 分块伏笔表

存储每个文本块的伏笔信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| run_id | VARCHAR(36) | 运行ID（外键） |
| foreshadowing_type | VARCHAR(255) | 伏笔类型 |
| anchor_text | TEXT | 锚点文本 |
| anchor_reason | TEXT | 锚点理由 |
| confidence | VARCHAR(255) | 置信度 |
| created_at | VARCHAR(50) | 创建时间 |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.1.9 chunk_locations - 分块地点表

存储每个文本块中出现的地点信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| location_name | VARCHAR(255) | 地点名称 |
| location_type | VARCHAR(255) | 地点类型 |
| run_id | VARCHAR(36) | 运行ID（外键） |
| novel_id | VARCHAR(255) | 小说ID |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

### 3.2 曲线数据表

#### 3.2.1 emotion_curve - 情感曲线表

存储情感曲线数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| pos_density | REAL | 正面情感密度 |
| neg_density | REAL | 负面情感密度 |
| net_density | REAL | 净情感密度 |
| smoothed_density | REAL | 平滑后情感密度 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.2.2 rhythm_curve - 节奏曲线表

存储节奏曲线数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| tension_proxy | REAL | 张力代理值 |
| tension_composite | REAL | 复合张力值 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.2.3 chunk_culture - chunk级文化词表密度表

存储 chunk 级文化词表密度数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| imagery_lexicon_density | REAL | chunk级意象词表命中密度 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

### 3.3 实体管理表

#### 3.3.1 entities - 实体表

存储消歧后的实体信息。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| entity_id | INTEGER | PRIMARY KEY | 实体ID（自增） |
| novel_id | VARCHAR(255) | NOT NULL | 小说ID |
| canonical | VARCHAR(255) | NOT NULL | 规范名称 |
| entity_type | VARCHAR(50) | NOT NULL | 实体类型（character/location/item等） |
| first_chunk | INTEGER | - | 首次出现的块ID |
| last_chunk | INTEGER | - | 最后出现的块ID |
| description | TEXT | - | 实体描述 |
| embedding_vector | vector(1536) | - | **向量嵌入（pgvector）** |
| confidence | REAL | DEFAULT 1.0 | 置信度 |
| run_id | VARCHAR(36) | FK | 运行ID（外键） |

**唯一约束**: (novel_id, canonical)
**向量索引**: idx_entities_vector (HNSW, cosine distance)

---

#### 3.3.2 entity_aliases - 实体别名表

存储实体的别名映射。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| alias_id | INTEGER | PRIMARY KEY | 别名ID（自增） |
| entity_id | INTEGER | NOT NULL | 实体ID（外键） |
| alias | VARCHAR(255) | NOT NULL | 别名 |
| alias_type | VARCHAR(50) | - | 别名类型 |
| source_chunk | INTEGER | - | 来源块ID |
| confirm_count | INTEGER | DEFAULT 1 | 确认次数 |
| run_id | VARCHAR(36) | FK | 运行ID（外键） |

**外键**: entity_id → entities(entity_id), run_id → analysis_runs(run_id)
**唯一约束**: (entity_id, alias)
**索引**: idx_entity_aliases_alias (alias)

---

#### 3.3.3 entity_relations - 实体关系表

存储消歧后的实体关系。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| rel_id | INTEGER | PRIMARY KEY | 关系ID（自增） |
| novel_id | VARCHAR(255) | NOT NULL | 小说ID |
| from_entity | INTEGER | NOT NULL | 发起实体ID（外键） |
| to_entity | INTEGER | NOT NULL | 目标实体ID（外键） |
| rel_type | VARCHAR(50) | NOT NULL | 关系类型 |
| rel_category | VARCHAR(20) | NOT NULL, DEFAULT 'interpersonal' | 关系类别：interpersonal（人际关系）/ hierarchical（层级关系） |
| first_chunk | INTEGER | - | 首次出现的块ID |
| last_chunk | INTEGER | - | 最后出现的块ID |
| tension | REAL | DEFAULT 0.0 | 张力指数 |
| is_active | INTEGER | DEFAULT 1 | 是否活跃 |
| run_id | VARCHAR(36) | FK | 运行ID（外键） |

**外键**: from_entity → entities(entity_id), to_entity → entities(entity_id), run_id → analysis_runs(run_id)
**唯一约束**: (novel_id, from_entity, to_entity, rel_type)

**关系类别说明**:
- `interpersonal`: 人际关系，如师徒、敌对、盟友、爱慕、家族、利益、主从
- `hierarchical`: 层级关系，如 belongs_to（属于）、member_of（成员）、leader_of（领袖）、affiliated_with（隶属于）

---

#### 3.3.4 entity_snapshots - 实体快照表

存储实体状态快照。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| snap_id | INTEGER | PRIMARY KEY | 快照ID（自增） |
| novel_id | VARCHAR(255) | NOT NULL | 小说ID |
| entity_id | INTEGER | NOT NULL | 实体ID（外键） |
| chunk_id | INTEGER | NOT NULL | 块ID |
| state_json | TEXT | - | 状态JSON |
| run_id | VARCHAR(36) | FK | 运行ID（外键） |

**外键**: entity_id → entities(entity_id), run_id → analysis_runs(run_id)
**唯一约束**: (novel_id, entity_id, chunk_id)
**索引**: idx_entity_snapshots_novel_chunk (novel_id, chunk_id)

---

#### 3.3.5 entity_registry - 实体注册表

存储实体的活跃状态快照（用于滚动记忆）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| entity_id | INTEGER | 实体ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| name | VARCHAR(255) | 实体名称 |
| role | VARCHAR(50) | 角色 |
| last_action | TEXT | 最后行为 |
| last_emotion | VARCHAR(50) | 最后情绪 |
| emotion_score | INTEGER | 情绪强度 |
| updated_at | VARCHAR(50) | 更新时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.3.6 character_appearances - 角色出现记录表

存储角色的身份线索信息（用于消歧）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID（外键关联chunks） |
| raw_name | VARCHAR(255) | 原始名称 |
| identity_clue | TEXT | 身份线索 |
| clue_type | VARCHAR(50) | 线索类型 |
| created_at | VARCHAR(50) | 创建时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)
**索引**: idx_character_appearances_chunk (chunk_id), idx_character_appearances_name (raw_name)

---

### 3.4 图谱权威表（v10.6 新增）

图谱权威表是知识图谱的持久化语义层，承担"角色是谁、别名是什么、当前关系是什么、证据来自哪里"的权威表达。

#### 3.4.1 graph_entities - 规范实体表

存储图谱中的规范实体（角色、群体、组织等）。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| entity_id | INTEGER | PRIMARY KEY | 实体ID（自增），稳定实体标识 |
| run_id | VARCHAR(36) | FK, NOT NULL | 运行ID（外键） |
| canonical_name | VARCHAR(255) | NOT NULL | 规范名称 |
| entity_type | VARCHAR(50) | NOT NULL | 实体类型：character/group/organization/location |
| first_seen_chunk | INTEGER | - | 首次出现的块ID |
| last_seen_chunk | INTEGER | - | 最近出现的块ID |
| primary_role_function | VARCHAR(50) | - | 主要叙事功能（Greimas六元素） |
| last_emotion_score | VARCHAR(50) | - | 最近情绪（五档枚举） |
| status | VARCHAR(20) | DEFAULT 'active' | 状态：active/merged/deprecated |
| source_confidence | REAL | - | 当前规范化置信度 |
| total_appearances | INTEGER | DEFAULT 0 | 总出现次数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 更新时间 |

**外键**: run_id → analysis_runs(run_id)
**索引**: idx_graph_entities_run (run_id), idx_graph_entities_name (canonical_name)
**唯一约束**: (run_id, canonical_name)

**设计说明**：
- `entity_id` 是图谱节点身份，不再用名字当主键
- `canonical_name` 可以变，但 `entity_id` 不能变
- 与现有 `entities` 表的区别：本表是图谱权威层，`entities` 是消歧中间层

---

#### 3.4.2 graph_entity_aliases - 别名权威表

存储实体的别名映射与证据链。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| alias_id | INTEGER | PRIMARY KEY | 别名ID（自增） |
| run_id | VARCHAR(36) | FK, NOT NULL | 运行ID（外键） |
| entity_id | INTEGER | FK, NOT NULL | 实体ID（外键关联 graph_entities） |
| alias | VARCHAR(255) | NOT NULL | 别名文本 |
| source_chunk_id | INTEGER | - | 证据来源块ID |
| evidence | TEXT | - | 原文证据片段 |
| confidence | REAL | - | 置信度 |
| source_type | VARCHAR(50) | - | 来源类型：phase1/phase3/disambiguation/manual |
| is_primary | INTEGER | DEFAULT 0 | 是否为规范名自身（0/1） |
| confirm_count | INTEGER | DEFAULT 1 | 确认次数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**外键**: entity_id → graph_entities(entity_id), run_id → analysis_runs(run_id)
**索引**: idx_graph_entity_aliases_entity (entity_id), idx_graph_entity_aliases_alias (alias)
**唯一约束**: (entity_id, alias)

**设计说明**：
- 这是角色别名的权威来源
- annotate 阶段的已知别名提示应优先查这张表
- 与现有 `entity_aliases` 的关系：本表是权威层，`entity_aliases` 在过渡期保留为兼容层

---

#### 3.4.3 graph_relation_events - 关系事件流表

存储图谱中的关系事件流，是图谱边的证据历史。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| relation_event_id | INTEGER | PRIMARY KEY | 关系事件ID（自增） |
| run_id | VARCHAR(36) | FK, NOT NULL | 运行ID（外键） |
| from_entity_id | INTEGER | FK, NOT NULL | 源实体ID |
| to_entity_id | INTEGER | FK, NOT NULL | 目标实体ID |
| relation_type | VARCHAR(50) | NOT NULL | 关系类型：师徒/敌对/盟友/爱慕/家族/利益/主从 |
| change_type | VARCHAR(50) | NOT NULL | 变化类型：强化/弱化/新建/断裂/无变化 |
| chunk_id | INTEGER | NOT NULL | 证据发生位置 |
| evidence | TEXT | - | 原文证据片段 |
| confidence | REAL | - | 本次抽取置信度 |
| source_relation_row_id | INTEGER | - | 对应 chunk_relations.id |
| directionality | VARCHAR(20) | DEFAULT 'directed' | 方向性：directed/symmetric |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**外键**: from_entity_id → graph_entities(entity_id), to_entity_id → graph_entities(entity_id), run_id → analysis_runs(run_id)
**索引**: idx_graph_relation_events_run (run_id), idx_graph_relation_events_entities (from_entity_id, to_entity_id)

**设计说明**：
- 图谱的"强证据性"主要体现在这里
- 每一条图谱关系都必须能追溯到具体 chunk 和 evidence
- 支持关系的时间维度追踪

---

#### 3.4.4 graph_relations_current - 当前关系快照表

存储当前生效的关系快照，供全链路稳定查询。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| relation_id | INTEGER | PRIMARY KEY | 关系ID（自增） |
| run_id | VARCHAR(36) | FK, NOT NULL | 运行ID（外键） |
| from_entity_id | INTEGER | FK, NOT NULL | 源实体ID |
| to_entity_id | INTEGER | FK, NOT NULL | 目标实体ID |
| current_type | VARCHAR(50) | NOT NULL | 当前生效关系类型 |
| first_seen_chunk | INTEGER | - | 首次建立位置 |
| last_seen_chunk | INTEGER | - | 最近更新位置 |
| change_count | INTEGER | DEFAULT 0 | 累积变化次数 |
| support_count | INTEGER | DEFAULT 0 | 支持事件数 |
| latest_event_id | INTEGER | FK | 最近一次支撑事件ID |
| tension_index | REAL | DEFAULT 0.0 | 张力指数 |
| is_active | INTEGER | DEFAULT 1 | 当前是否生效（0/1） |
| updated_at | TIMESTAMP | - | 更新时间 |

**外键**: from_entity_id → graph_entities(entity_id), to_entity_id → graph_entities(entity_id), latest_event_id → graph_relation_events(relation_event_id), run_id → analysis_runs(run_id)
**唯一约束**: (run_id, from_entity_id, to_entity_id)

**设计说明**：
- annotate 阶段查活跃角色和已知关系
- aggregate 阶段构图
- diagnose / report 阶段生成摘要和可视化

---

#### 3.4.5 graph_checkpoints - 图谱断点恢复表

存储图谱投影的断点信息，用于断点恢复。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| checkpoint_id | INTEGER | PRIMARY KEY | 断点ID（自增） |
| run_id | VARCHAR(36) | FK, NOT NULL | 运行ID（外键） |
| chunk_id | INTEGER | NOT NULL | 已处理到的块ID |
| alias_checkpoint_id | INTEGER | - | 关联的别名断点ID |
| entities_count | INTEGER | DEFAULT 0 | 已投影实体数 |
| relations_count | INTEGER | DEFAULT 0 | 已投影关系数 |
| status | VARCHAR(20) | DEFAULT 'active' | 状态：active/completed/abandoned |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**外键**: run_id → analysis_runs(run_id)
**索引**: idx_graph_checkpoints_run (run_id)

**设计说明**：
- 与 alias checkpoint 统一管理
- 只保存处理进度，不保存 NetworkX 对象
- resume 时从最近的 checkpoint 继续投影

---

### 3.5 全局数据表

#### 3.5.1 global_stats - 全局统计表

存储全局统计指标。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| stat_name | VARCHAR(255) | 统计项名称（主键） |
| stat_value | REAL | 统计值 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**主键**: stat_name

---

#### 3.5.2 global_context - 全局上下文表

存储小说的全局上下文信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| novel_id | VARCHAR(255) | 小说ID（主键） |
| novel_title | VARCHAR(500) | 小说标题 |
| core_characters | TEXT | 核心角色列表（JSON格式） |
| world_setting | TEXT | 世界观设定 |
| updated_at | VARCHAR(50) | 更新时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**主键**: novel_id

---

#### 3.5.3 cloud_analysis - 云端诊断表

存储云端模型的宏观诊断结果。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| novel_id | VARCHAR(255) | 小说ID |
| foreshadow_rate | REAL | 伏笔率 |
| arc_scores | TEXT | 故事弧评分（JSON格式） |
| narrative_type | VARCHAR(100) | 叙事类型 |
| topic_labels | TEXT | 主题标签（JSON格式） |
| diagnosis | TEXT | 诊断报告 |
| value_logic_type | VARCHAR(50) | 价值逻辑类型 |
| value_logic_reason | TEXT | 价值逻辑理由 |
| power_stance_score | INTEGER | 权力立场评分 |
| power_stance_reason | TEXT | 权力立场理由 |
| common_people_dignity | INTEGER | 庶民尊严评分 |
| dignity_reason | TEXT | 庶民尊严理由 |
| cultural_depth_score | INTEGER | 文化深度评分 |
| cultural_depth_reason | TEXT | 文化深度理由 |
| narrative_arc_type | VARCHAR(50) | 叙事弧线形态 |
| run_id | VARCHAR(36) | 运行ID（外键） |

---

### 3.6 辅助数据表

#### 3.6.1 chunk_summaries - 块摘要表

存储每个文本块的摘要信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| summary | TEXT | 摘要内容 |
| created_at | VARCHAR(50) | 创建时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)

---

#### 3.6.2 chunk_embeddings - 块嵌入向量表

存储文本块的向量嵌入（**pgvector**）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| embedding_vector | vector(1536) | **向量嵌入（pgvector 类型）** |
| created_at | VARCHAR(50) | 创建时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)
**向量索引**: idx_chunk_embeddings_vector (HNSW, cosine distance)

---

#### 3.6.3 paragraph_embeddings - 段落嵌入向量表

存储候选 chunk 内 paragraph 的局部证据向量（**pgvector**）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| run_id | VARCHAR(36) | 运行ID（主键，外键） |
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| paragraph_index | INTEGER | 段落序号（主键） |
| paragraph_text | TEXT | 段落文本 |
| local_start_char | INTEGER | 段落在 chunk 文本内的起始字符偏移 |
| local_end_char | INTEGER | 段落在 chunk 文本内的结束字符偏移 |
| global_start_char | INTEGER | 段落在全文中的起始字符偏移 |
| global_end_char | INTEGER | 段落在全文中的结束字符偏移 |
| embedding_vector | vector(1536) | 段落向量嵌入 |
| created_at | VARCHAR(50) | 创建时间 |

**主键**: (run_id, chunk_id, paragraph_index)
**外键**: (chunk_id, run_id) → chunks(chunk_id, run_id), run_id → analysis_runs(run_id)
**索引**: idx_paragraph_embeddings_run_id (run_id), idx_paragraph_embeddings_run_chunk (run_id, chunk_id)

---

#### 3.6.4 chunk_summary_embeddings - 摘要嵌入向量表

存储文本块摘要的向量嵌入（**pgvector**）。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| chunk_id | INTEGER | 块ID（主键，外键关联chunks） |
| run_id | VARCHAR(36) | 运行ID（外键） |
| novel_id | VARCHAR(255) | 小说ID |
| summary_text | TEXT | 摘要文本 |
| embedding_vector | vector(1536) | **向量嵌入（pgvector 类型）** |
| created_at | VARCHAR(50) | 创建时间 |

**外键**: chunk_id → chunks(chunk_id), run_id → analysis_runs(run_id)
**向量索引**: idx_chunk_summary_embeddings_vector (HNSW, cosine distance)

---

#### 3.6.5 token_usage - Token使用统计表

存储API调用的Token使用统计。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| novel_id | VARCHAR(255) | 小说ID |
| chunk_id | INTEGER | 块ID |
| task_type | VARCHAR(100) | 任务类型 |
| call_type | VARCHAR(50) | 调用类型（local/cloud） |
| model | VARCHAR(100) | 模型名称 |
| prompt_tokens | INTEGER | 提示Token数 |
| completion_tokens | INTEGER | 完成Token数 |
| total_tokens | INTEGER | 总Token数 |
| created_at | VARCHAR(50) | 创建时间 |
| run_id | VARCHAR(36) | 运行ID（外键） |

**索引**: idx_token_usage_novel (novel_id), idx_token_usage_task (novel_id, task_type)

---

#### 3.6.5 disambig_checkpoint - 消歧检查点表

存储人名消歧过程中的检查点数据，用于断点恢复。

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| run_id | VARCHAR(36) | PRIMARY KEY | 运行ID（主键，外键关联 analysis_runs） |
| alias_map | TEXT | NOT NULL | 别名映射表（JSON格式） |
| updated_at | REAL | NOT NULL | 更新时间戳 |

**外键**: run_id → analysis_runs(run_id) ON DELETE CASCADE

**说明**：
- `alias_map` 存储内存中的别名映射，格式为 `{"别名": "正式名"}` 的 JSON 对象
- 增量消解过程中每隔 N 块保存一次，确保任务中断后可恢复
- 最终消解完成后，数据被 `apply_alias_corrections` 批量写入各标注表

---

#### 3.6.7 model_interactions - 模型交互记录表

存储模型调用的详细交互记录，用于调试和分析。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 记录ID（主键，自增） |
| chunk_id | INTEGER | 块ID |
| run_id | VARCHAR(36) | 运行ID（外键） |
| interaction_type | VARCHAR(255) | 交互类型 |
| phase | VARCHAR(255) | 阶段 |
| attempt_number | INTEGER | 尝试次数 |
| model_name | VARCHAR(255) | 模型名称 |
| model_provider | VARCHAR(255) | 模型提供商 |
| prompt | TEXT | 提示词 |
| response | TEXT | 响应内容 |
| thinking | TEXT | 思考过程 |
| response_chars | INTEGER | 响应字符数 |
| thinking_chars | INTEGER | 思考字符数 |
| has_thinking | INTEGER | 是否有思考过程（0/1） |
| status | VARCHAR(255) | 状态 |
| error_message | TEXT | 错误信息 |
| created_at | TIMESTAMP | 创建时间 |
| duration_ms | INTEGER | 耗时（毫秒） |

**外键**: run_id → analysis_runs(run_id)
**索引**: idx_model_interactions_run (run_id), idx_model_interactions_chunk (chunk_id)

---

## 四、向量搜索

### 4.1 pgvector 使用

```python
from sqlalchemy import select
from src.storage.models import Entity, ChunkEmbedding

# 余弦相似度搜索
query_vector = [0.1, 0.2, ...]  # 1536维向量

stmt = select(Entity).order_by(
    Entity.embedding_vector.cosine_distance(query_vector)
).limit(10)

# L2 距离搜索
stmt = select(ChunkEmbedding).order_by(
    ChunkEmbedding.embedding_vector.l2_distance(query_vector)
).limit(10)
```

### 4.2 向量索引

使用 HNSW（Hierarchical Navigable Small World）索引进行高效的近似最近邻搜索：

```sql
-- 创建索引
CREATE INDEX idx_chunk_embeddings_vector 
ON chunk_embeddings 
USING hnsw (embedding_vector vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- 查询时设置 ef 参数（影响搜索精度）
SET hnsw.ef_search = 100;
```

---

## 五、表关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                         核心数据层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐                                               │
│  │    chunks    │ ◄─────────────────────────────────────────┐   │
│  │  (PK:chunk_id)│                                          │   │
│  └──────┬───────┘                                          │   │
│         │                                                  │   │
│         ├──────────────────────┬───────────────────────┐   │   │
│         ▼                      ▼                       ▼   │   │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐│
│  │ chunk_style  │      │chunk_annotation│    │chunk_culture ││
│  │  (FK:chunk_id)│      │  (FK:chunk_id) │    │  (FK:chunk_id)││
│  └──────────────┘      └──────────────┘      └──────────────┘│
│                                                                 │
│         ├──────────────────────┬───────────────────────┐      │
│         ▼                      ▼                       ▼      │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐ │
│  │chunk_characters│    │chunk_relations│     │chunk_dialogues│ │
│  │  (FK:chunk_id) │    │  (FK:chunk_id) │     │  (FK:chunk_id) │ │
│  └──────────────┘      └──────────────┘      └──────────────┘ │
│                                                                 │
│         ├──────────────────────┼───────────────────────┐      │
│         ▼                      ▼                       ▼      │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐ │
│  │ chunk_topics │      │emotion_curve │      │ rhythm_curve │ │
│  │  (FK:chunk_id)│      │  (FK:chunk_id) │     │  (FK:chunk_id) │ │
│  └──────────────┘      └──────────────┘      └──────────────┘ │
│                                                                 │
│         ├──────────────────────┼───────────────────────┐      │
│         ▼                      ▼                       ▼      │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐ │
│  │chunk_summaries│     │chunk_embeddings│    │character_    │ │
│  │  (FK:chunk_id)│      │  (FK:chunk_id) │     │appearances   │ │
│  │              │       │ [pgvector]    │      │  (FK:chunk_id)│ │
│  └──────────────┘      └──────────────┘      └──────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         实体管理层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐                                               │
│  │   entities   │ ◄─────────────────────────────────────────┐   │
│  │(PK:entity_id)│                                          │   │
│  │ [pgvector]   │                                          │   │
│  └──────┬───────┘                                          │   │
│         │                                                  │   │
│         ├──────────────────────┬───────────────────────┐   │   │
│         ▼                      ▼                       ▼   │   │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐│
│  │entity_aliases│      │entity_relations│    │entity_snapshots││
│  │(FK:entity_id)│      │(FK:from_entity)│    │(FK:entity_id) ││
│  └──────────────┘      │(FK:to_entity)  │     └──────────────┘│
│                        └──────────────┘                       │
│                                                                 │
│  ┌──────────────┐                                              │
│  │entity_registry│                                              │
│  │(FK:chunk_id) │                                              │
│  └──────────────┘                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         全局数据层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐  │
│  │ global_stats │      │global_context│      │cloud_analysis│  │
│  │(PK:stat_name)│      │(PK:novel_id) │      │              │  │
│  └──────────────┘      └──────────────┘      └──────────────┘  │
│                                                                 │
│  ┌──────────────┐                                              │
│  │ token_usage  │                                              │
│  │(PK:id)       │                                              │
│  └──────────────┘                                              │
│                                                                 │
│  ┌──────────────┐                                              │
│  │disambig_     │                                              │
│  │checkpoint    │                                              │
│  │(PK:run_id)   │                                              │
│  └──────────────┘                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、数据流说明

### 6.1 预处理阶段（preprocess）

1. **文本读取** → `chunks` 表
2. **语言风格计算** → `chunk_style` 表
3. **传统文化计算** → `chunk_culture` 表

### 6.2 标注阶段（annotate）

1. **本地模型标注** → `chunk_annotation` 表
2. **人物提取** → `chunk_characters` 表
3. **关系提取** → `chunk_relations` 表
4. **对话提取** → `chunk_dialogues` 表
5. **人名消歧** → `entities`, `entity_aliases` 表
6. **消歧检查点** → `disambig_checkpoint` 表（增量保存）
7. **上下文更新** → `global_context`, `entity_registry` 表
8. **身份线索记录** → `character_appearances` 表

### 6.3 聚合阶段（aggregate）

1. **情感曲线计算** → `emotion_curve` 表
2. **节奏曲线计算** → `rhythm_curve` 表
3. **全局统计计算** → `global_stats` 表
4. **人物关系图构建** → `graph_relations_current` 表

### 6.4 主题建模阶段（topic-model）

1. **LDA主题提取** → `chunk_topics` 表

### 6.5 诊断阶段（diagnose）

1. **云端模型诊断** → `cloud_analysis` 表

---

## 七、索引策略

| 表名 | 索引名 | 字段 | 用途 |
|------|--------|------|------|
| analysis_runs | idx_analysis_runs_novel | novel_id | 按小说查询运行 |
| analysis_runs | idx_analysis_runs_status | status | 按状态查询 |
| chunks | idx_chunks_run_id | run_id | 按运行ID查询 |
| entity_aliases | idx_entity_aliases_alias | alias | 别名快速查找 |
| entity_snapshots | idx_entity_snapshots_novel_chunk | novel_id, chunk_id | 最近快照查询 |
| token_usage | idx_token_usage_novel | novel_id | 按小说统计Token |
| token_usage | idx_token_usage_task | novel_id, task_type | 按任务统计Token |
| character_appearances | idx_character_appearances_chunk | chunk_id | 按块查询角色 |
| character_appearances | idx_character_appearances_name | raw_name | 按名称查询角色 |
| chunk_embeddings | idx_chunk_embeddings_vector | embedding_vector | **向量相似度搜索 (HNSW)** |
| entities | idx_entities_vector | embedding_vector | **向量相似度搜索 (HNSW)** |

---

## 八、数据完整性约束

### 8.1 外键约束

所有外键约束已启用，确保：
- 无法删除被引用的父记录
- 无法插入不存在的外键值
- 级联删除：`ON DELETE CASCADE`

### 8.2 唯一约束

| 表名 | 约束字段 | 说明 |
|------|----------|------|
| entities | (novel_id, canonical) | 同一小说内实体名称唯一 |
| entity_aliases | (entity_id, alias) | 同一实体的别名唯一 |
| entity_relations | (novel_id, from_entity, to_entity, rel_type) | 同一关系类型唯一 |
| entity_snapshots | (novel_id, entity_id, chunk_id) | 同一位置的快照唯一 |

---

## 九、版本历史

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-03-12 | 初始版本，包含22个核心表 |
| 1.1 | 2026-03-13 | Schema重构：emotional_valence/emotion_score改为五档枚举、event_type改为三分类、role_function改为Greimas六元素、新增action_type字段、删除emotion/tone字段 |
| 2.0 | 2026-03-15 | **PostgreSQL 迁移**：迁移至 PostgreSQL，添加 pgvector 支持，所有表添加 run_id 外键，移除 SQLite 特有语法 |
| 2.1 | 2026-03-16 | **消歧检查点**：新增 disambig_checkpoint 表，支持人名消歧断点恢复 |
| 2.2 | 2026-03-18 | **实体类型与关系**：entity_relations 表新增 rel_category 字段，支持区分人际关系和层级关系；新增 group_appearances 和 organization_appearances 字段提取群体和组织名称 |
| 2.3 | 2026-03-19 | **ID系统统一优化**：明确区分 task_id（外部API使用）和 run_id（内部实现使用），task_id 是 run_id 的前8位字符，外部API只暴露 task_id
| 2.4 | 2026-03-29 | **文档同步更新**：补充缺失的4张表文档 - chunk_foreshadowing（分块伏笔表）、chunk_locations（分块地点表）、chunk_summary_embeddings（摘要嵌入向量表）、model_interactions（模型交互记录表）
