# 中文网络小说量化分析工作流

双模型协作工作流 · 理论与设计文档

v10.1

> **版本历史**
> - v10.3 (2026-03-16): 人名消歧三项修复 - 变体匹配机制、延迟写入与Checkpoint机制、防误合并规则
> - v10.2 (2026-03-13): Schema 重构 - emotional_valence/emotion_score 改为五档枚举、event_type 改为三分类、role_function 改为 Greimas 六元素、新增 action_type、删除 emotion/tone 字段、三幕比例改用 tension_composite 计算
> - v10.1 (2026-03-12): 新增角色身份线索字段、家族关系判断规则、停用词表扩充
> - v10.0: 初始版本

---

## 一、项目概览

### 1.1 项目定位

本工作流针对中文网络小说（单部体量 50–300 万字）进行系统性量化分析，覆盖叙事结构、人物关系、情感曲线、语言风格、主题内容、传统文化传承、叙事价值立场七个核心维度，每个维度的指标均有明确的叙事学、计量文体学或语料库语言学理论来源。

### 1.2 核心架构

**「本地小模型 + 云端大模型」双模型协作体系，代码负责确定性计算，两种模型各司其职：**

| **执行者** | **职责** | **特点** |
|------------|----------|----------|
| 本地小模型（Qwen3.5-9B Q6） | 高频批量任务 | 零成本，承担 90% 调用量 |
| 云端大模型（Claude API） | 低频高难度任务 | 按需调用，负责全局推理与综合判断 |
| 代码（Python） | 所有确定性计算 | 不依赖模型，100% 准确 |

### 1.3 技术栈

| **用途** | **工具** | **说明** |
|----------|----------|----------|
| 本地推理 | llama.cpp + Qwen3.5-9B Q6_K | OpenAI 兼容接口，约 8GB 显存 |
| 结构化输出 | outlines + Pydantic | 生成阶段强制 JSON 合法，无需重试 |
| 中文分词 | jieba + HanLP | jieba 速度快，HanLP 精度高 |
| 数据处理 | pandas / polars | 标注结果聚合与指标计算 |
| 人物关系图 | networkx + pyvis | 图论指标计算与交互可视化 |
| 主题建模 | gensim LDA | 主题提取，云端模型辅助命名 |
| 统计检验 | scipy.stats | T 检验、Spearman 相关系数 |
| 可视化 | plotly + streamlit | 交互式分析看板 |
| 持久化 | PostgreSQL + SQLAlchemy ORM | 生产级数据库，支持并发与扩展 |
| 数据库迁移 | Alembic | Schema 版本管理 |
| 云端推理 | Claude API（Anthropic） | 低频高难度任务，单书费用 < $2 |
| 文本向量化 | BAAI/bge-large-zh-v1.5（可选） | 语义索引与伏笔候选检索 |
| NER 初筛 | shibing624/bert4ner-base-chinese（可选） | 人名候选提取初筛 |

---

## 二、项目工作流

### 2.1 整体流程图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           中文网络小说量化分析工作流                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────┐    ┌─────────────────────────┐    ┌─────────┐              │
│  │ ① 预处理 │───▶│ ②+③ 标注与消解（嵌入式） │───▶│④ 指标聚合│              │
│  │  (代码)  │    │   本地模型 + 云端模型    │    │  (代码)  │              │
│  └─────────┘    └─────────────────────────┘    └─────────┘              │
│       │                    │                          │                  │
│       ▼                    ▼                          ▼                  │
│  ┌─────────┐    ┌─────────────────────────┐    ┌─────────┐              │
│  │分块JSON │    │标注数据库 + 全局别名表   │    │七维指标 │              │
│  └─────────┘    └─────────────────────────┘    └─────────┘              │
│                                                     │                   │
│                                                     ▼                   │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐              │
│  │⑧可视化  │◀───│⑦跨书比较│◀───│⑥深度分析│◀───│⑤主题建模│              │
│  │(Streamlit)   │(代码+云端)│   │(云端模型)│    │(LDA+云端)│              │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘              │
│                                                                         │
│  【标注与消解流程】                                                       │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐             │
│  │ 标注块N   │──▶│提取新人名│──▶│增量消解  │──▶│更新别名表│──▶ 标注块N+1│
│  │(本地模型) │   │          │   │(本地模型)│   │          │             │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘             │
│                                     ...                                │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐            │
│  │全部完成  │──▶│最终消解  │──▶│匿名消歧  │──▶│更新数据库│            │
│  │          │   │(云端模型)│   │(云端模型)│   │          │            │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘            │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 阶段概览

| **阶段** | **执行者** | **输入** | **输出** |
|----------|------------|----------|----------|
| ① 预处理 | 代码 | 原始 txt | 分块 JSON + 语言风格指标 |
| ② 人名消解（嵌入式） | 本地模型 + 云端模型 | 标注结果中的人物名称 | 全局别名表（alias_map） |
| ③ 逐块标注 | 本地模型（批量） | 每块文本 + 别名表 + 滚动摘要 | 结构化标注数据库 |
| ④ 指标聚合 | 代码 | 标注数据库 | 七维指标 DataFrame |
| ⑤ 主题建模 | 代码（LDA）+ 云端模型 | 全书分词语料 | 主题分布 + 主题命名 |
| ⑥ 深度分析 | 云端模型 | 全书摘要 + 指标汇总 | 伏笔验证 / 人物弧度 / 宏观诊断 / 叙事价值立场 |
| ⑦ 跨书比较 | 代码 + 云端模型 | 多书指标 | T检验显著性 + 对比报告 |
| ⑧ 可视化看板 | Streamlit | 全部数据 | 交互式分析界面 |

> **注**：阶段②人名消解嵌入在阶段③标注流程中执行，采用「边标注边消解」策略，详见 3.2 节。

---

## 三、工作流详解

### 3.1 阶段一：预处理

#### 理论依据

预处理阶段是整个工作流的基础，涉及文本分块、语言风格指标计算、情感词典加载等确定性操作。分块策略需保证语义完整性，避免在段落中途截断。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 原始 txt 文件 | 分块 JSON、候选人名列表、语言风格指标 |

#### 处理逻辑

**① 文本分块**

分块以 2000 字为目标，到达目标字数后等当前段落结束再切，不在段落中途截断。实际块大小因段落长短在 1800–2500 字之间浮动。

```python
def split_into_chunks(text, target=2000):
    paragraphs = text.split('\n')
    chunks, current, current_len = [], [], 0
    for para in paragraphs:
        current.append(para)
        current_len += len(para)
        if current_len >= target:
            chunks.append('\n'.join(current))
            current, current_len = [], 0
    if current:
        chunks.append('\n'.join(current))
    return chunks
```

**语义分块（已实现）**：使用 Qwen3-Embedding 向量模型实现语义边界分块，替代固定字数分块：
- 集成 Qwen3-Embedding 模型（通过 llama.cpp 服务，端口8081）
- 实现语义相似度计算（EmbeddingClient.compute_similarity）
- 基于语义边界进行分块（SemanticChunker 类）
- 章节检测作为硬边界
- **优势**：语义分块可提升叙事连贯性
- **配置项**：`use_semantic_chunking`、`semantic_threshold`、`semantic_max_chars`

**② 语言风格指标计算**

语言风格指标由代码直接计算，包括：
- MTLD（词汇多样性）
- TTR（型例比）
- 平均句长、句长标准差
- D值（句子离散度）
- 停顿密度（句子破碎度）
- 对话占比
- 五感词密度
- 比喻密度

**③ 情感词典加载**

加载 BosonNLP 情感词典或 NTUSD 简体版，用于后续词汇情感密度计算。

**④ jieba 分词集成**

后续规划在预处理阶段集成 jieba 分词，提升中文分词准确性：
- 扩展 jieba 分词至预处理阶段
- 添加自定义词典支持
- 优化分词结果缓存机制
- **解决的问题**：边缘角色漏检

---

### 3.2 阶段二：人名消歧（嵌入式）

#### 理论依据

从标注结果中提取人物名称，识别同一人物的不同称呼，构建全局别名表。人名消歧是人物关系分析的前提，消歧质量直接影响后续人物网络构建的准确性。

**架构说明**：人名消歧并非独立阶段，而是嵌入在标注流程中执行，采用「边标注边消解」的策略，确保早期发现的别名能及时用于后续标注。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 标注结果中提取的人物名称 | 全局别名表（alias_map） |

#### 处理逻辑

**① 两阶段消解机制**

| **阶段** | **触发时机** | **执行模型** | **目的** |
|----------|--------------|--------------|----------|
| 增量消解 | 每N块标注完成后 | 本地模型（Qwen3.5-9B） | 及时识别新出现的别名，用于后续标注 |
| 最终消解 | 全部标注完成后 | 云端模型（DeepSeek-Reasoner） | 全局一致性校验，修正增量消解的遗漏 |

**② 增强机制**

| **机制** | **说明** |
|----------|----------|
| 上下文例句注入 | 消歧时为每个候选人名提供其在原文中的上下文例句（最多3句），优先抓取包含别名提示词的句子 |
| 滚动别名表 | 标注时传入当前已知的别名表（alias_map），让模型在标注阶段就使用正式名 |
| 增量消解 | 每N块触发一次增量消解，确保早期别名及时收录并用于后续标注 |
| 历史锚点注入 | 增量消解时告知模型当前知识库中已存在的核心角色，解决"新名字无法合并到老名字"的问题 |
| 防误判说明 | 消歧 Prompt 中包含说明，防止共现句（如"伯安对贺铮说"）和说话者误识别导致的错误合并 |
| 隐性马甲识别 | 支持识别外貌描述性称呼（如"灰衣人"）与正式名字的对应关系 |
| 匿名消歧 | 全量消歧后，对匿名占位名（如`匿名_C1_0`）进行专门消歧，提供完整上下文（前一块+当前块+后一块）识别真实身份 |

**③ 别名提示词词表**

消解时优先抓取包含以下关键词的句子作为上下文例句：

| **类别** | **关键词** |
|----------|------------|
| 名字关系 | 名、字、叫、就是、称号、全名、号、绰号、别号、雅号 |
| 身份揭示 | 真容、真面目、原形、化身、伪装、真实身份、竟是、原来是 |

**④ 变体匹配机制（v10.3 新增）**

解决三字及以上人名的短称匹配问题（如"贺重明"→"重明"）：

| **规则** | **说明** |
|----------|----------|
| 变体生成 | 对三字及以上人名，生成去掉首字的短称变体 |
| 变体查重 | 短称变体不与已知人名集合冲突时才使用 |
| 匹配优先级 | 全名优先匹配，变体次之 |

```python
def _get_name_variants(name: str, name_set: set[str]) -> list[str]:
    """生成候选名的字符串变体。"""
    variants = [name]
    if len(name) >= 3:
        short = name[1:]  # "贺重明" → "重明"
        if short not in name_set:
            variants.append(short)
    return variants
```

**⑤ 延迟写入与 Checkpoint 机制（v10.3 新增）**

解决增量消解错误污染标注数据的问题：

| **机制** | **说明** |
|----------|----------|
| 延迟写入 | 增量消解仅更新内存中的 `memory_alias_map`，不写入数据库 |
| Checkpoint 保存 | 每隔 N 块将 `memory_alias_map` 保存到 `disambig_checkpoint` 表 |
| 最终写入 | 最终消解完成后，调用 `apply_alias_corrections` 批量修正所有标注表 |
| 断点恢复 | 任务中断后可从最近的 Checkpoint 恢复，避免重复消解 |

**数据流**：
```
增量消解 → memory_alias_map (内存)
                ↓
         Checkpoint 保存 (每N块)
                ↓
最终消解 → apply_alias_corrections → 数据库批量更新
```

**⑥ 防误合并规则（v10.3 新增）**

消歧 Prompt 中新增以下规则，防止错误合并：

| **规则** | **说明** |
|----------|----------|
| 共现排除 | 同一句子中同时出现的两个人名不能合并为同一人 |
| 命名句保护 | "X叫Y"、"X就是Y"等命名句中，X和Y是同一人的不同称呼 |
| 辈分排除 | 存在"爷爷/父亲/儿子"等辈分关系描述的两人名不能合并 |

---

### 3.3 阶段三：逐块标注

#### 理论依据

本地模型对每个分块独立分析，返回结构化 JSON。标注内容涵盖情感极性、叙事结构、人物快照、人物关系变化、对话归因等维度。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 每块文本 + 别名表 + 滚动摘要 | 结构化标注数据库 |

#### 处理逻辑

**① 标注模式选择**

系统支持两种标注模式：

| **模式** | **调用次数** | **适用场景** | **配置项** |
|----------|--------------|--------------|------------|
| 单次调用模式 | 1次 | 快速标注，基础字段 | `two_phase_annotation.enabled=false` |
| 双次调用模式 | 2次 | 精细分析，含伏笔分析 | `two_phase_annotation.enabled=true` |

**双次调用模式详解**：

| **阶段** | **功能** | **输出字段** |
|----------|----------|--------------|
| Phase1 | 基础标注 | emotional_valence、event_type、pivot_moment、cliffhanger、characters、relations、dialogues、chunk_summary |
| Phase2 | 伏笔分析 | has_foreshadowing、foreshadowing_type、anchor_text、anchor_reason、confidence |

**执行方式**：

| **方式** | **说明** | **配置项** |
|----------|----------|------------|
| 串行执行 | Phase1 完成后执行 Phase2，Phase2 可使用 Phase1 的摘要 | `two_phase_annotation.parallel=false` |
| 并行执行 | Phase1 和 Phase2 同时执行，提高处理速度 | `two_phase_annotation.parallel=true` |

**② 重试与容错机制**

双次调用模式下，Phase1 和 Phase2 拥有独立的重试机制：

| **阶段** | **本地重试** | **云端Fallback** | **总机会** |
|----------|--------------|------------------|------------|
| Phase1 | 3次 | 3次 | 6次 |
| Phase2 | 3次 | 3次 | 6次 |

重试触发条件：
- 连接错误（ConnectionError）
- 超时错误（TimeoutError）
- API错误（APITimeoutError、APIConnectionError）
- JSON解析失败（Phase2）

**③ 本地模型批量标注**

对每个分块输出以下结构化字段：

```json
{
  "emotional_valence": "strong_positive|mild_positive|neutral|mild_negative|strong_negative",
  "event_type": "冲突|铺垫|转折",
  "pivot_moment": true|false,
  "cliffhanger": true|false,
  "has_foreshadowing": true|false,
  "foreshadowing_type": "causal|thematic|null",
  "foreshadowing_desc": "伏笔描述",
  "characters": [{
    "name": "正式名",
    "role_function": "主体|客体|发送者|接收者|帮助者|反对者",
    "action": "核心行为动词",
    "action_type": "战斗|逃跑|对话|决策|移动|情感|其他",
    "emotion_score": "strong_positive|mild_positive|neutral|mild_negative|strong_negative"
  }],
  "relations": [{
    "from": "人物A",
    "to": "人物B",
    "type": "师徒|敌对|盟友|爱慕|家族|利益|主从",
    "change": "强化|弱化|新建|断裂|无变化"
  }],
  "dialogues": [{
    "speaker": "说话人"
  }],
  "character_appearances": [{
    "raw_name": "文中原始称呼",
    "identity_clue": "身份线索描述",
    "clue_type": "none|self_introduction|named_by_other|alias_revealed|appearance_desc"
  }],
  "chunk_summary": "本块核心事件总结（50字以内，必须包含出场人名）"
}
```

**字段变更说明（v10.2）：**
- `emotional_valence`：三分类改为五档枚举（strong_positive/mild_positive/neutral/mild_negative/strong_negative）
- `emotion_score`：连续整数改为五档枚举，与 `emotional_valence` 对齐
- `event_type`：五分类改为三分类（冲突/铺垫/转折），三幕比例改用 `tension_composite` 计算
- `role_function`：五分类改为 Greimas 六元素（主体/客体/发送者/接收者/帮助者/反对者）
- `relations.type`：新增「主从」分类
- `action_type`：新增字段，七分类枚举（战斗/逃跑/对话/决策/移动/情感/其他）
- `character_appearances`：新增字段，记录角色身份线索
- `chunk_summary`：新增字段，要求必须包含出场人名
- `emotion`：删除字段（冗余）
- `tone`：删除字段（下游无法聚合）

**② 滚动摘要与上下文传递（已实现）**

标注过程中维护滚动摘要，为后续块提供上下文信息。已实现以下三种机制：

| **机制** | **说明** | **解决的问题** |
|----------|----------|----------------|
| 全局状态注入（Global Context Padding） | 在每次 Prompt 的系统提示词中，固定写入几百字的"全局核心信息"（基础设定、核心主角名单） | 无论当前 Chunk 多短，LLM 都不会偏离大框架 |
| 滚动记忆流（Rolling Memory） | 把上一个 Chunk 的文本尾部（最后 200 字）或抽取结果摘要，作为 `<Previous_Context>` 标签拼接到当前请求中 | 解决"人祖三人继续向前走"中"三人"到底是谁的连贯性问题 |
| 动态实体注册表（Dynamic Entity Registry） | 利用 PostgreSQL 架构，在查询当前 Chunk 前，从数据库提取刚活跃过的角色状态（极简 JSON 格式），塞进当前 Prompt 的余量里 | 充当 LLM 的"短期工作记忆" |

**实现状态**：
- 新增 `global_context` 表存储全局核心信息
- 新增 `entity_registry` 表存储角色活跃状态快照
- `extract_global_context` 函数从前N个chunk提取核心角色和设定
- `_build_messages` 方法支持 `global_context`、`prev_tail_text`、`active_entities` 参数注入
- `run_annotate` 函数集成全局上下文提取和滚动记忆注入

**③ 主动排除在 Schema 之外的字段**

以下字段由代码计算，不占用模型输出：
- 句长 / 词频 / 对话占比 / 五感词 / 比喻密度 / 传统文化词密度
- 情感曲线形态 / 人物网络密度 / 三幕比例（代码从原始信号聚合）
- 伏笔兑现率 / 情节原型归类 / 人物弧度评分 / 叙事价值立场（全书维度，交给云端）

---

### 3.4 阶段四：指标聚合

#### 理论依据

代码从本地模型输出的原始信号字段聚合得出最终指标。核心原则：代码能算的绝不交给模型，模型只输出「原始信号字段」。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 标注数据库 | 七维指标 DataFrame |

#### 处理逻辑

**① 指标来源分层**

| **来源** | **执行方式** | **可靠性** | **典型指标** |
|----------|--------------|------------|--------------|
| 来源一：代码直接计算 | 预处理阶段并行运行，无需模型 | 100%（确定性算法） | MTLD、句长、TTR、对话占比、D值、停顿密度 |
| 来源二：模型输出原始信号，代码聚合 | 模型输出逐块字段，事后 pandas/networkx 聚合 | 中（依赖模型信号质量） | 情感曲线形态、高潮间距、人物网络密度 |
| 来源三：必须由模型直接输出 | 语义理解不可或缺，代码无法替代 | 低–中 | emotional_valence、pivot_moment、event_type、伏笔标记、人物关系、叙事价值立场 |

**② A/B 两类指标分开报告**

| **类型** | **维度** | **测量方法** | **报告方式** | **可跨书比较** |
|----------|----------|--------------|--------------|----------------|
| A 类：可量化指标 | 语言风格（全部） | 代码直接计算 | 数值，可排序 | ✅ 可以 |
| A 类：可量化指标 | 叙事结构（表面） | 代码：章节长度方差、对话占比分布 | 数值 | ✅ 可以 |
| B 类：解释性判断 | 情感弧度、人物深度 | 云端模型采样分析 | 自然语言，不出分值 | ❌ 不跨书比较 |
| B 类：解释性判断 | 主题内容 | LDA + 云端命名 | 主题列表 + 说明 | ⚠️ 相对比较 |

---

### 3.5 阶段五：主题建模

#### 理论依据

**LDA 隐狄利克雷分配（Blei et al. 2003）**：无监督概率生成模型，基于词袋假设，认为每篇文档是多个隐藏主题的混合，每个主题是词语的概率分布。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 全书分词语料 | 主题分布 + 主题命名 |

#### 处理逻辑

**① LDA 参数设置**

| **参数** | **单书分析** | **多书横向比较** | **说明** |
|----------|--------------|------------------|----------|
| 主题数 K | 20–30 | 100–150 | 单书主题太多会产生高度相似的冗余主题 |
| 文档单元 | 每块（~2000字） | 每章 | 按章节切分更符合叙事单元 |
| 每主题输出词数 | 前 15 词 | 前 20 词 | |
| 迭代次数 | 500 | 1000 | 多书语料更大，需更多迭代 |

**② 云端模型命名**

LDA 跑出主题词后，交由云端模型对每个主题进行语义命名和归纳，替代传统的人工标注环节。

---

### 3.6 阶段六：深度分析

#### 理论依据

深度分析阶段由云端模型承担，负责需要全局推理与综合判断的高难度任务。云端模型在结构事件和原始文本上做推理，不在聚合数值上做推理。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 全书摘要 + 指标汇总 | 伏笔验证 / 人物弧度 / 宏观诊断 / 叙事价值立场 |

#### 处理逻辑

**① 伏笔兑现验证**

本地模型只能标记「此处有伏笔」，无法判断后续是否兑现。云端模型在全书标注完成后，接收伏笔摘要列表，在全书结构摘要中检索对应回收场景，输出兑现率评分。

**② 人物弧度综合评估**

接收主要人物的全书行为/情感序列摘要，评估弧度完整性，判断性格转变是否有充分铺垫，输出自然语言评估报告。人物弧度是解释性判断，不以数值评分为结论。

**③ 宏观结构诊断**

云端模型输入由以下六类结构化信息构成：

```python
cloud_input = {
    'pivot_blocks': get_top_k_by_semantic_shift(chunk_vecs, k=10),
    'pivot_moments': get_pivot_moment_summaries(annotations),
    'high_tension_segments': get_high_tension_chunks(chunk_style, top_pct=0.1),
    'relation_snapshots': get_relation_snapshots(chunk_relations, thirds=3),
    'foreshadowing_list': get_foreshadowing_list(chunk_annotation),
    'opening_summary': summarize_chapters(chunks, range(0, 5)),
    'closing_summary': summarize_chapters(chunks, range(-5, 0)),
}
```

**核心原则：** 云端模型在结构事件和原始文本上做推理，不在聚合数值上做推理。输出是解释性判断（自然语言），不是对数字的再统计。

**④ 叙事价值立场评估**

在宏观结构诊断的同一次调用中，追加以下三个判断问题，不单独开启调用：

| **判断维度** | **具体问题** | **评分标准** |
|--------------|--------------|--------------|
| 强欺弱场景的叙事倾向 | 书中强者欺压弱者场景，叙事情感倾向是谴责、中立还是欣赏？ | 0=欣赏强者 2=中立 5=谴责压迫 |
| 普通人的叙事尊严 | 普通配角受难时是被认真书写还是仅作背景板？ | 0=纯背景板 2=偶有描写 5=认真书写 |
| 核心价值逻辑 | 全书最终传递的核心价值逻辑是善义有价值、强者为王还是混合型？ | 文字说明 + 代表性依据段落 |

**设计说明：** 叙事价值立场是对叙事道德判断方向的测量，与传统文化维度不重叠。传统文化维度测量的是「用了什么文化资源」，叙事价值立场测量的是「站在谁那边」，两者可以独立变化。

---

### 3.7 阶段七：跨书比较

#### 理论依据

跨书比较阶段接收各书指标摘要和 T 检验结果，生成跨书对比分析报告，找出共性规律和类型差异。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 多书指标 | T检验显著性 + 对比报告 |

#### 处理逻辑

**① T检验**

对 A 类可量化指标进行 T 检验，判断差异是否具有统计显著性。

**② 云端模型生成对比报告**

接收各书指标摘要和 T 检验结果，生成跨书对比分析报告。

---

### 3.8 阶段八：可视化看板

#### 理论依据

可视化看板基于 Streamlit 框架，提供交互式分析界面，展示各维度指标和曲线。

#### 输入输出

| **输入** | **输出** |
|----------|----------|
| 全部数据 | 交互式分析界面 |

#### 处理逻辑

展示内容包括：
- 情感曲线（词汇情感密度、节奏代理信号、情感极性序列、语义跳跃曲线）
- 叙事结构指标
- 人物关系统计
- 主题分布
- 云端诊断结果

---

## 四、维度与指标

> **详细指标说明请参阅：《中文网络小说指标参考手册.md》**
>
> 该手册包含 52 个指标的完整说明，涵盖：
> - 字段名、取值范围、高低含义
> - 数据来源、计算方法
> - 理论来源与学术依据
> - A类/B类指标分类

### 4.1 七大分析维度概览

| **维度** | **核心内容** | **主要指标类型** |
|----------|--------------|------------------|
| 维度一：叙事结构 | 故事骨架的完整性、节奏分布与悬念设计 | 三幕比例、事件密度、高潮间距、悬念保持率、伏笔兑现率 |
| 维度二：情感曲线 | 四条独立曲线（词汇情感密度、节奏代理、情感极性、语义跳跃） | 曲线形态、正负词汇比、节奏波动幅度、情感恢复速度 |
| 维度三：人物关系 | 社会网络分析量化人物结构地位 | 人物度中心性、关系网络密度、人物功能覆盖率、弧度完整性 |
| 维度四：语言风格 | 全部由代码确定性计算，100% 可重复验证 | MTLD、TTR、句长、D值、五感词密度、比喻密度 |
| 维度五：主题内容 | LDA 主题建模 + 云端模型命名 | 主题分布、主题语义命名 |
| 维度六：传统文化传承 | 测量文本使用的传统文化资源 | 儒道佛民间典故词密度、四字成语密度、古典句式比例 |
| 维度七：叙事价值立场 | 测量叙事者「站在谁那边」 | 强欺弱叙事倾向、普通人叙事尊严、核心价值逻辑 |

### 4.2 情感曲线平滑方法

当前 `smoothed_density` 使用滑动窗口平滑，存在滞后问题。后续规划采用**傅里叶变换**方案（参考 Matthew Jockers《畅销书写作密码》）：

| **步骤** | **说明** |
|----------|----------|
| 离散傅里叶变换 | 对情感密度数组做 DFT，转换到频域 |
| 保留低频分量 | 保留宏观故事弧（低频），过滤逐句波动（高频噪声） |
| 逆变换 | 做逆变换得到贴近真实叙事节奏的平滑曲线 |

### 4.3 情感维度升维规划

当前 `emotion_score` 为单标量（-5 到 5），后续规划升维为三维向量：

| **维度** | **说明** |
|----------|----------|
| 内在情绪 | 人物内心真实情绪状态 |
| 外在表现 | 人物外在情绪表达方式 |
| 对目标对象的情感 | 对特定对象的情感倾向 |

### 4.4 关系张力指数规划

当前 `relations` 表记录关系类型和变化，后续规划增加 `tension_index`（张力指数）字段：

- **量化场景**：盟友关系但也可能张力极高（各怀鬼胎）
- **存储方案**：引入 Neo4j（图数据库）或在内存中构建更复杂的 NetworkX 动态图

### 4.5 指标类型说明

| 类型 | 含义 | 可跨书比较 | 典型来源 |
|------|------|-----------|---------|
| **A类** | 结果确定，可重复验证，误差来源明确 | ✅ 可以，可做T检验 | 代码计算、代码聚合 |
| **A类有限** | 依赖模型信号，稳定性较高但非100%确定 | ⚠️ 有限比较 | 本地模型聚合 |
| **B类** | 叙事质量是解释性概念，不追求精确分值 | ❌ 不出精确分值 | 云端模型综合判断 |

---

## 五、词表设计

### 5.1 词表分类

#### 高优先级词表（工作流基础运行必需）

| **编号** | **词表名称** | **用途** | **建议规模** |
|----------|--------------|----------|--------------|
| 词表1 | 情感词典 | 词汇情感密度计算 | 约 2000–5000 词条 |
| 词表2 | 语义类别词表 | 语言风格维度：武功武器、身体部件、人物关系等10类 | 每类 50–100 词，共约 500–1000 词条 |
| 词表3 | 五感词表 | 语言风格维度：视觉/听觉/嗅觉/味觉/触觉 | 每类 30–50 词，共约 150–250 词条 |
| 词表4 | 战斗词表 | 异常检测：tension 分值与战斗词密度矛盾时触发云端复核 | 约 100–150 词条 |
| 词表5 | 专有词典（jieba用户词典） | 防止仙侠/修仙专有词汇被 jieba 切错 | 视语料而定，通常 300–800 词条 |
| 词表6 | 停用词补充表 | 过滤网络小说特有虚词和无意义词 | 约 100–200 词条 |
| 词表7 | 别名提示词词表 | 人名消解时优先抓取含提示词的句子（已实现18词） | 约 18 词条 |

#### 低优先级词表（传统文化维度，词表完成后启用）

| **编号** | **词表名称** | **用途** | **建议规模** |
|----------|--------------|----------|--------------|
| 词表A | 儒家伦理词表 | 传统文化维度：仁义礼智信等儒家核心概念 | 约 80–120 词条 |
| 词表B | 道家意象词表 | 传统文化维度：天道、阴阳、无为等道家概念 | 约 80–120 词条 |
| 词表C | 佛教词汇词表 | 传统文化维度：因果、轮回、业力等佛教词汇 | 约 60–100 词条 |
| 词表D | 江湖民间伦理词表 | 传统文化维度：义气、恩怨、门规等民间文化词汇 | 约 100–150 词条 |
| 词表E | 典故词表 | 传统文化维度：历史典故词、历史人名、经典典故短语 | 约 300–500 词条 |

#### 无需词表的指标（可直接启用）

| **指标** | **计算方式** | **说明** |
|----------|--------------|----------|
| 四字成语密度 | 正则匹配四字格词 ÷ 总词数 | jieba 词性标注后过滤，无需词表 |
| 古典句式比例 | 正则匹配五言七言格式句型 | 正则可算，无需词表 |
| 比喻密度 | 正则匹配「像/如/似/般/宛若/犹如」 | 正则可算，无需词表 |
| 对话占比 | 正则提取引号内字数 ÷ 总字数 | 正则可算，无需词表 |
| 叙事价值立场 | 合并入宏观诊断云端调用 | 无需词表，只需修改 prompt 文件 |

### 5.2 词表建设原则

1. **覆盖高频核心词即可**：各类词表词条数建议 50–150 条，追求召回率而非完美覆盖
2. **词表只做粗筛**：定量工作由词表完成，真实性校验交给云端模型（定性）
3. **贴近网络小说实际语言**：儒道佛三类词表不应直接照搬学术辞典，需收录网络小说实际使用频率高的通俗化用词
4. **典故词表可复用现有资源**：可参考现有典故词典（如《汉语成语词典》《中华典故大全》）导入后筛选，无需从零建设

### 5.3 词表建设优先级路线图

| **阶段** | **目标** | **需要建设的词表** | **可以启用的维度** |
|----------|----------|---------------------|---------------------|
| 第一阶段 | 工作流基础跑通 | 词表1–7（高优先级） | 原有五个维度全部启用 |
| 第二阶段 | 零成本扩展 | 无需词表 | 额外启用叙事价值立场（维度七） |
| 第三阶段 | 传统文化维度 | 词表A–E（低优先级，可分批建设） | 逐步启用传统文化维度（维度六） |

---

## 六、API设计

> **详细 API 文档请参阅：《API文档.md》**
>
> 该文档包含完整的 API 端点说明、请求/响应格式、错误处理等内容。

### 6.1 API 端点概览

| **类别** | **端点** | **说明** |
|----------|----------|----------|
| 小说管理 | `POST /api/novels/upload` | 上传小说文件 |
| 小说管理 | `GET /api/novels/` | 列出所有小说 |
| 小说管理 | `DELETE /api/novels/{id}` | 删除小说 |
| 分析任务 | `POST /api/novels/{id}/analyze` | 启动分析任务 |
| 分析任务 | `POST /api/novels/{id}/reanalyze` | 重新分析（创建新版本） |
| 分析任务 | `GET /api/novels/{id}/status` | 查询分析进度 |
| 分析任务 | `GET /api/novels/{id}/analyses` | 获取所有分析版本 |
| 结果查询 | `GET /api/novels/{id}/emotion-curve` | 获取情感曲线 |
| 结果查询 | `GET /api/novels/{id}/rhythm-curve` | 获取节奏曲线 |
| 结果查询 | `GET /api/novels/{id}/characters` | 获取人物统计 |
| 结果查询 | `GET /api/novels/{id}/topics` | 获取主题分布 |
| 结果查询 | `GET /api/novels/{id}/diagnosis` | 获取云端诊断 |
| 聚合指标 | `GET /api/novels/{id}/metrics/*` | 获取各维度聚合指标 |

---

## 七、双模型分工设计

### 7.1 分工原则

| **维度** | **本地小模型（Qwen3.5-9B）** | **云端大模型（Claude API）** |
|----------|------------------------------|------------------------------|
| 调用频次 | 高频（每块一次，数百次） | 低频（每部书 5–15 次） |
| 任务难度 | 结构化提取、局部语义理解 | 跨章节推理、综合判断 |
| 上下文长度 | 单块 ~2000 字 + 滚动摘要 | 全书摘要 + 关键片段 |
| 成本 | 零（本地 GPU） | 按 token 计费，单书 < $2 |
| 输出格式 | 严格 JSON（Pydantic 校验） | 自然语言报告 / JSON 均可 |

### 7.2 本地模型职责（高频任务）

| **任务** | **说明** |
|----------|----------|
| 逐块情感与事件标注 | 对每个分块独立分析，返回结构化 JSON |
| 人名消解（增量） | 每N块触发一次，识别新出现的别名，及时用于后续标注 |
| 对话归因 | 识别每段对话的说话人及语气类型 |

### 7.3 云端模型职责（低频高难度任务）

| **任务** | **说明** |
|----------|----------|
| 人名消解（最终） | 全部标注完成后进行全局一致性校验，修正增量消解的遗漏 |
| 伏笔兑现验证 | 接收伏笔摘要列表，检索对应回收场景，输出兑现率评分 |
| 人物弧度综合评估 | 评估弧度完整性，判断性格转变是否有充分铺垫 |
| 宏观结构诊断 | 在结构事件和原始文本上做推理，输出解释性判断 |
| LDA 主题命名 | 对每个主题进行语义命名和归纳 |
| 多书横向比较报告 | 生成跨书对比分析报告 |
| 叙事价值立场评估 | 判断叙事道德立场（合并入宏观诊断） |

---

## 八、模型质量保障

### 8.1 噪声来源

| **噪声类型** | **具体表现** | **影响程度** |
|--------------|--------------|--------------|
| 一致性噪声 | 同一段文字不同批次运行可能得到不同分类结果 | 中 |
| 边界噪声 | emotional_valence 在正负边界的段落上分类不稳定 | 中 |
| 语境噪声 | 「她笑着走向死亡」被表面词义误导 | 高 |

### 8.2 控制手段

| **手段** | **说明** |
|----------|----------|
| 相对排序替代绝对分值 | 情感曲线分析只需要相对判断，滑动平均平滑单点噪声 |
| 云端模型抽样校验 | 每 50 块随机抽 5 块同时送云端做三分类，计算一致率 |
| 规则层兜底异常检测 | 若 emotional_valence=neutral 但战斗词密度极高，触发云端复核 |
| 关键块多次采样取中位数 | 对高张力区域的关键块做 3 次独立推理取中位数 |
| 锚点示例注入 Prompt | 在 prompt 中提供具体参考样例，覆盖边界情况 |

### 8.3 各指标可信度评级

| **指标** | **可信度** | **来源** |
|----------|------------|----------|
| 词汇情感密度曲线（趋势） | ★★★★★ | 代码 + 词典 |
| 语义跳跃曲线 | ★★★★★ | Embedding |
| 语言风格全部指标 | ★★★★★ | 代码 |
| 节奏代理信号 | ★★★★ | 代码 |
| 情感极性序列（三分类） | ★★★★ | 本地模型 |
| 事件类型分类 | ★★★ | 本地模型 |
| pivot_moment 标记 | ★★★ | 本地模型 |
| 伏笔识别 | ★★ | 本地模型 |
| 叙事价值立场 | ★★★ | 云端模型 |
| 人物弧度 | ★★★ | 云端模型 |
| 情感曲线形态 | ★★★ | 云端模型 |

---

## 九、理论基础与本土化

### 9.1 理论来源汇总

| **理论 / 研究** | **年代** | **来源维度** | **在本工作流中的应用** |
|------------------|----------|--------------|------------------------|
| Labov 叙事结构模型 | 1972 | 叙事结构 | event_type 标签分类依据 |
| Freytag 三角 | 1863 | 叙事结构 | 三幕比例、高潮位置基准 |
| Brewer 悬念理论 | 1996 | 叙事结构 | cliffhanger 标记依据 |
| Vonnegut 情节形态 | 1952 | 情感曲线 | 情感曲线形状的理论框架 |
| Reagan et al. 六种原型情节 | 2016 | 情感曲线 | 情感曲线形态识别的六类标准 |
| Russell 情感环形模型 | 1980 | 情感曲线 | 情感评分的二维化理论依据 |
| Propp 叙事功能 | 1928 | 人物关系 | role_function 分类依据 |
| Greimas 行动元模型 | 1966 | 人物关系 | 人物功能覆盖率计算依据 |
| 社会网络分析（SNA） | — | 人物关系 | 人物度中心性、网络密度等图论指标 |
| Burrows 计量文体学 | 1987 | 语言风格 | 高频虚词文体指纹 |
| McCarthy MTLD | 2005 | 语言风格 | 词汇多样性主要指标 |
| Lynott & Connell 感官词研究 | 2009 | 语言风格 | 五感词密度计算依据 |
| Wang et al. 语料库研究 | 2023 | 语言风格 | 句子离散度、破碎度、语义类别词 |
| Blei et al. LDA | 2003 | 主题内容 | 主题建模算法 |
| 钟敬文民俗学 | — | 传统文化 | 民间文化类词表设计依据 |
| 叶嘉莹古典意象研究 | — | 传统文化 | 古典意象词表设计依据 |
| 陈平原武侠小说研究 | — | 传统文化 | 江湖伦理词表设计依据 |

### 9.2 理论本土化

上述来自西方的理论直接套用中文网络小说存在系统性偏差，需要本土化处理：

| **偏差类型** | **具体表现** | **本土化处理** |
|--------------|--------------|----------------|
| 情感曲线模式不同 | 网络小说受「爽文」机制驱动，典型曲线是高频小正峰叠加、整体持续上升 | 将「高潮密度」而非「高潮高度」作为主要衡量维度 |
| 伏笔逻辑不同 | 中国古典叙事「草蛇灰线」更强调意象呼应，不要求严格逻辑兑现 | 将伏笔分为 causal（因果型）和 thematic（意象型）两类分别验证 |
| 人物功能密度更高 | 网络小说一章内可能出现多次功能切换 | 改用「功能覆盖率 × 章节数」归一化 |
| 传统文化词表本土化 | 儒道佛三类词表不应直接照搬学术辞典 | 收录网络小说实际使用频率高的通俗化用词 |
| 叙事价值立场参照系 | 网络小说整体偏向强者逻辑 | 「中立」基准应基于类型内部比较 |

---

## 十、已知局限

| **问题** | **影响维度** | **处理建议** |
|-----------|--------------|--------------|
| 极低频路人角色识别缺失 | 人物关系 | 接受损失，主要人物覆盖率 > 95% |
| 纯代词指代难以消歧 | 情感归因 | 标记为「未知视角」，不纳入人物弧度计算 |
| 跨 50 章以上的超长伏笔 | 伏笔兑现率 | 云端分析时注入更多章节摘要 |
| 仙侠/修仙功法体系词汇分词错误 | 语言风格 | 预置专有词表覆盖高频奇葩词 |
| TTR 受长度影响 | 词汇多样性 | 跨书比较时须等长切分，单书用 MTLD |
| LDA 主题边界模糊 | 主题内容 | 增加人工复核，对主题重叠度高的进行合并 |
| 情感绝对分值不可精确比较 | 情感曲线 | 以趋势和排序为主，不直接报告精确数值 |
| 理论指标均来自西方语料基准 | 所有维度 | 只做同类型小说横向比较，不与西方基准对齐 |
| 传统文化词表覆盖不完整 | 传统文化 | 词表只做粗筛，云端校验文化内涵真实性 |
| 叙事价值立场判断主观性较高 | 叙事价值立场 | 多书比较时看排序趋势，不报告精确分值差异 |

---

v10.0 · 工作流理论设计文档 · 七大分析维度 · 双模型协作架构
